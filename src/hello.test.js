import {describe, it } from 'bun:test'

describe('bun:test', () => {
  it('works', () => {
    expect(1).toBe(1)
    expect(1).toBe(1)
    expect(1).toBe(1)
    expect(1).toBe(1)
    expect(1).toBe(1)
    expect(1).toBe(1)
  })
})
// bun test  atau bun test --watch hello.test.js